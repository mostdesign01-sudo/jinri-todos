{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults, drag-by-handle, and the collapse commands the webview invokes.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "allow-set-collapsed",
    "allow-is-collapsed"
  ]
}
